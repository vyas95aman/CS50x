#include <stdio.h>

int main(void)
{
    for (int i = 0; i <= 3; i ++)
    {
        // printf("i is %i", i);
        printf("#\n");
    }
}

// Want 3 #, instead get 4 with <= 3
// printf is good tool, print i with # -> see that we start counting at 0 to 3 = 4, change to <3

// Can also use debug50 ./buggy in desktop remote access
// Can also debug in here with debug console


